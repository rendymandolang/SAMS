@extends('layouts.app',['title'=>$entry->document_number.' · SAMS'])
@section('body')<div class="app-shell">@include('partials.sidebar')<main class="main"><header class="page-header"><div><p class="eyebrow">Journal Voucher</p><h1>{{ $entry->document_number }}</h1><p>{{ $entry->journal_date }} · {{ $entry->memo }}</p></div><div style="display:flex;gap:8px"><a class="button secondary" href="{{ route('accounting.print',$entry->id) }}" target="_blank">Print</a>@if($entry->status==='draft'&&auth()->user()->hasPermission('accounting.post'))<form method="POST" action="{{route('accounting.post',$entry->id)}}">@csrf<button class="button primary">Post Journal</button></form>@endif</div></header>@if(session('status'))<div class="notice">{{session('status')}}</div>@endif<section class="card"><div class="detail-grid"><div class="detail-box"><span class="muted">Status</span><div class="value">{{str($entry->status)->title()}}</div></div><div class="detail-box"><span class="muted">Adjustment</span><div class="value">{{$entry->is_adjustment?'Yes':'No'}}</div></div><div class="detail-box"><span class="muted">Debit</span><div class="value">Rp {{number_format($entry->total_debit,0,',','.')}}</div></div><div class="detail-box"><span class="muted">Credit</span><div class="value">Rp {{number_format($entry->total_credit,0,',','.')}}</div></div></div><div class="table-wrap" style="margin-top:18px"><table><thead><tr><th>Sub Department</th><th>Account</th><th>Remark</th><th>Debit</th><th>Credit</th></tr></thead><tbody>@foreach($lines as $l)<tr><td>{{$l->department_name?:'-'}}</td><td><strong>{{$l->code}}</strong> · {{$l->account_name}}</td><td>{{$l->description}}</td><td>Rp {{number_format($l->debit,0,',','.')}}</td><td>Rp {{number_format($l->credit,0,',','.')}}</td></tr>@endforeach</tbody></table></div></section></main></div>@endsection
