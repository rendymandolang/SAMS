@extends('layouts.app',['title'=>'Consolidation · SaS'])
@section('body')<div class="app-shell">@include('partials.sidebar')<main class="main"><header class="page-header"><div><p class="eyebrow">SaS · Group Accounting</p><h1>Entity Consolidation</h1><p>Group structure, currency translation, eliminations, and consolidated statements.</p></div></header>@if(session('status'))<div class="notice">{{session('status')}}</div>@endif @if($errors->any())<div class="error">{{$errors->first()}}</div>@endif
<section class="grid two-columns"><article class="card"><h2>New consolidation group</h2><form method="POST" action="{{route('accounting.consolidation.store')}}">@csrf<label class="field"><span class="label">Group name</span><input class="input" name="name" required></label><label class="field"><span class="label">Presentation currency</span><input class="input" name="presentation_currency" value="{{$company->currency}}" maxlength="3" required></label><button class="button primary">Create group</button></form></article><article class="card"><h2>Consolidation groups</h2>@forelse($groups as $group)<div class="detail-box" style="display:flex;justify-content:space-between;align-items:center;margin-bottom:8px"><div><strong>{{$group->name}}</strong><br><span class="muted">{{$group->presentation_currency}}</span></div><a class="button secondary inline" href="{{route('accounting.consolidation.show',$group->id)}}">Open</a></div>@empty<p class="muted">No consolidation group yet.</p>@endforelse</article></section>
</main></div>@endsection
